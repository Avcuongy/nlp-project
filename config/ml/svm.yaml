# SVM Model Configuration
model: svm
type: ml

# GridSearchCV
grid_search:
  # Cross-Validation settings
  cv: 5

  # Scoring metric
  scoring: f1_macro
  n_jobs: -1

  # Hyperparameter grid
  param_grid:
    C: [0.1, 1.0, 10.0, 100.0]
    kernel: ["linear", "rbf"]
    gamma: ["scale", "auto"]
